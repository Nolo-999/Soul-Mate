import PersonaCreatePage from './pages/PersonaCreatePage';

export default function App() {
  // 当前只有一个页面：人格创建
  // TODO: 后续接入 react-router 路由（欢迎页 / MBTI / 模板库 / 工坊 / 对话 / 记忆 / 登录）
  return <PersonaCreatePage />;
}
